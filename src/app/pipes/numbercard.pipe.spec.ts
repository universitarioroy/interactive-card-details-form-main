import { NumbercardPipe } from './numbercard.pipe';

describe('NumbercardPipe', () => {
  it('create an instance', () => {
    const pipe = new NumbercardPipe();
    expect(pipe).toBeTruthy();
  });
});
