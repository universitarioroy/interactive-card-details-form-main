import { MonthvalidDirective } from './monthvalid.directive';

describe('MonthvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new MonthvalidDirective();
    expect(directive).toBeTruthy();
  });
});
